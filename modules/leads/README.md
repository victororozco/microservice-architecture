# VidaPlatform - Lead Module

## Environments

Create `.envs` folder:
```sh
mkdir .envs
```

Create `.envs` files:
```sh
touch .backend
touch .frontend
```

Content of `.backend` file:
```sh
PORT=8001
```